# Chintu Next Codex Prompt (Stage 13B)

Paste the block below verbatim into a fresh Codex (or any continuation
agent) session. It is self-contained — you do not need to re-explain
the project.

---

## The prompt

> You are Codex continuing Chintu OS work from a fresh thread, picking
> up Stage 13B. The repo is `C:\Users\Chintu\Desktop\test`. The
> previous (Claude) session shipped Stage 13A and wrote handoff files
> for you.
>
> Read these files first, in this exact order:
>
> 1. `CHINTU_CLAUDE_TO_CODEX_HANDOFF.md` — handoff brief.
> 2. `CHINTU_STAGE_13_PROGRESS.md` — Stage 13B priority list.
> 3. `CHINTU_OPEN_FIRST.md` — single-page orientation.
> 4. `CHINTU_SAFETY_INVARIANTS.md` — canonical safety rules.
> 5. `CHINTU_CONNECTOR_POLICY.md` — connector ladder rules.
>
> Then orient with:
>
> ```powershell
> git status --short
> git log --oneline -10
> powershell -ExecutionPolicy Bypass -File scripts\chintu-action-planner.ps1
> node scripts/chintu-action-planner.test.js
> ```
>
> Hard rules (do NOT violate):
>
> - Never edit `app.js`, `index.html`, `styles.css`, `sw.js`,
>   `coach.js`, `manifest.webmanifest`, `privacy.html`,
>   `functions/api/coach.js`.
> - Never bump the service-worker `CACHE_NAME`.
> - Never activate Telegram, Discord, webhooks, cloud sync, phone,
>   voice cloning, paid APIs, network egress, memory-wiki, or any
>   external automation.
> - Never store a real secret or token in the repo.
> - Never push. Push is the founder's call.
> - Never modify `chintu-safety-boundary.test.js` to "permit" a
>   protected file edit.
>
> Stage 13B scope (work in this order, smallest first):
>
> 1. A tracked snapshot of the action queue (so the dashboard can
>    link to a stable Markdown file, not the gitignored one). Suggest
>    `CHINTU_ACTION_QUEUE_TRACKED.md` regenerated by a small extra
>    step in `scripts/chintu-action-planner.ps1`, or by a separate
>    `scripts/chintu-action-queue-snapshot.ps1`. Strip volatile
>    fields (timestamp) before writing.
> 2. Fixture-driven categoriser unit test that asserts which action
>    ids end up in the top 5 under different `(unpushed, tree dirty,
>    runtime status, bridge status)` combinations. Read-only fixture
>    files under `scripts/fixtures/` are fine.
> 3. `CHINTU_APPROVAL_AUDIT.md` (tracked, append-only) — schema only;
>    a tiny PowerShell helper that appends one row when the founder
>    runs `approve <id>` by hand.
> 4. ~~Update `scripts/chintu-control-room-index.ps1` to surface the
>    new planner outputs under a "Chintu Planner" section. Re-run
>    the generator.~~ **DONE in the visibility slice** — section
>    already added; skip this item.
> 5. Add a "Run the planner" section to `CHINTU_OPEN_FIRST.md`.
> 6. Append a cycle-8 section to
>    `CHINTU_CLAUDE_SURVIVAL_HANDOFF.md`.
>
> Validate after each meaningful slice:
>
> ```powershell
> node scripts/chintu-action-planner.test.js
> node scripts/chintu-doc-link-integrity.test.js
> node scripts/chintu-command-map.test.js
> node scripts/chintu-generated-files-map.test.js
> node scripts/chintu-medical-claims.test.js
> node scripts/chintu-no-network-egress.test.js
> powershell -ExecutionPolicy Bypass -File scripts\chintu-release-guard.ps1
> ```
>
> Commit one safe slice per concept. Suggested commit subject for the
> bundled Stage 13B work:
>
> ```
> chore: add Chintu planner snapshot, audit log, and dashboard surface (Stage 13B)
> ```
>
> Stop conditions:
>
> - Any integrity test fails and you cannot safely fix the root
>   cause.
> - A safety boundary would be crossed.
> - A push or external send would be required.
> - You hit your own usage / context limit. If so, write a fresh
>   handoff to `CHINTU_CODEX_TO_CLAUDE_HANDOFF.md` and stop.
>
> Final safety footer whenever BALA is mentioned:
>
> > BALA is a health-awareness companion. It does not diagnose,
> > treat, predict, prevent, replace doctors, or provide emergency
> > monitoring.

---

## What this prompt is NOT for

- Pushing (founder-only).
- BALA app file edits (founder-only).
- Lifting any parked surface.
- Adding a real Telegram/Slack/Discord/Gmail send.

## BALA safety footer

BALA is a health-awareness companion. It does not diagnose, treat,
predict, prevent, replace doctors, or provide emergency monitoring.
